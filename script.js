import { clamp, map, mix, smoothstep } from './src/modules/num.js';
import { drawInfo } from './src/modules/drawbox.js';

// Your animation logic here
// (Use the content that you have in the `<script>` tag in your HTML)
